# fruit_classifier
This project built a Convolutional Neural Netwok (CNN) using PyTorch to classify fruit images. The model was rtrained on the Fruits-360 dataset and can recognize 131 different types of fruits.
